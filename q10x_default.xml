<?xml version="1.0" encoding="UTF-8"?>
<manifest>

  <!-- Github remote -->
  <remote  name="github"
           fetch="https://github.com/" />

  <!-- DU-X GitHub -->
  <remote  name="du-x"
           revision="q10x"
           fetch="https://github.com/DU-X" />

  <!-- GitLab remote -->
  <remote name="gitlab"
          fetch="https://gitlab.com/" />

  <!-- Build -->
  <project path="build/make" name="android_build" remote="du-x" >
    <copyfile src="core/root.mk" dest="Makefile" />
    <linkfile src="CleanSpec.mk" dest="build/CleanSpec.mk" />
    <linkfile src="buildspec.mk.default" dest="build/buildspec.mk.default" />
    <linkfile src="core" dest="build/core" />
    <linkfile src="envsetup.sh" dest="build/envsetup.sh" />
    <linkfile src="target" dest="build/target" />
    <linkfile src="tools" dest="build/tools" />
  </project>
  <project path="build/soong" name="android_build_soong" remote="du-x" >
    <linkfile src="root.bp" dest="Android.bp" />
    <linkfile src="bootstrap.bash" dest="bootstrap.bash" />
  </project>

  <!-- Hardware -->
  <project path="hardware/qcom/camera" name="android_hardware_qcom_camera" remote="du-x" />
  <project path="hardware/qcom/gps" name="android_hardware_qcom_gps" remote="du-x" />

  <!-- Manifest -->
  <project path="manifest" name="android_manifest" remote="du-x" revision="q10x-caf" />

  <!-- Packages -->
  <project path="packages/apps/Bluetooth" name="android_packages_apps_Bluetooth" remote="du-x" />
  <project path="packages/apps/Settings" name="android_packages_apps_Settings" remote="du-x" />

  <!-- Prebuilts -->
  <project path="prebuilts/openssl" name="LineageOS/android_prebuilts_openssl" remote="github" revision="lineage-17.0" />
  <project path="prebuilts/tools-lineage" name="LineageOS/android_prebuilts_tools-lineage" remote="github" revision="lineage-17.0" />

  <!-- Vendor -->
  <project path="vendor/du" name="android_vendor_du" remote="du-x" />
  <project path="vendor/extras" name="android_vendor_extras" remote="du-x" />

</manifest>
